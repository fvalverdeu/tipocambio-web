export interface IChangeRequest {
  monedaorigen: string;
  monedadestino: string;
  monto: number;
}

export interface IChangeResponse {
  monedaorigen: string;
  monedadestino: string;
  monto: number;
}
